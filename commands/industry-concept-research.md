# industry-concept-research

## 质量门控（QC）计算约定

- 规则计算：按 `qc-rules.vX.json` 计算（并通过 `qc-rules.schema.json` 验证）。
- 运行时默认配置：`contracts/qc-rules.v2.0.0.json`。
- 结构定义来源：`contracts/qc-rules.schema.json`（仅用于 schema）。
